spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/luxury_car_db
    username: raghavjha
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082

jwt:
  secret: "this_is_a_very_long_secret_key_for_dev_only_123456"
  expirationSeconds: 86400

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
